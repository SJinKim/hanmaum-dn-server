# Explore Agent

Use when: scanning repo structure, understanding unfamiliar modules, impact analysis.

Tasks:
- Map all files in a feature directory
- Identify all callers of a given service/method
- Check for duplicate logic across features
- Summarize git log for a specific path

Return: structured summary with file paths, key findings, no code edits.
