package com.hanmaum.dn.app.features.training.domain

/** Grouping used by the catalog and the admin overview. Mirrors ck_training_category. */
enum class TrainingCategory {
    FOUNDATION,
    DISCIPLESHIP,
    BIBLE,
    MINISTRY,
    MISSION,
}
